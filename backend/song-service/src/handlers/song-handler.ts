import { SongServiceHandlers } from "../generated/song/SongService";

//Implementation for RPC, as defined in song.proto
const handlers: SongServiceHandlers = {
    GetSong: (call, callback) => {
        
    }
}